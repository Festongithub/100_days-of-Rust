#!/usr/bin/python3

import json

basic_entry ={
        'id': 256,
        'title': 'Mathematcis for SE',
        'tags': ('DiveInto Math'),
        'published': True,
        'comments_link': None,
        }


with open('entry.json', 'w', encoding='utf-8') as f:
    json.dump(basic_entry, f, indent=2)

personal_info = {
        'name': 'Terry Kimani',
        'age': 34,
        'course': 'Economics',
        'Language': ('C', 'C++', 'Python', 'Java'),
        }

with open('student.json', 'w', encoding='utf-8') as p:
    json.dumps(personal_info)
