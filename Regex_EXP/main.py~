#!/usr/bin/python3
import re

pattern = r'ab*'
text = 'a boy is a man in the golden era'

matches = re.findall(pattern, text)
print(matches)

nums = r'a+'
nums_text = 'abc aaa, aaaaa'
text_nums = re.findall(nums, nums_text)
print(text_nums)
